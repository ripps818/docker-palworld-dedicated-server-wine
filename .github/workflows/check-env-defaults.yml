name: check-env-var-defaults

on:
  pull_request:
    paths:
      - 'Dockerfile'
      - 'default.env'
      - 'docs/ENV_VARS.md'
      - '.github/scripts/check-env-defaults.sh'

jobs:
  check-env-defaults:
    name: docs/ENV_VARS.md and Dockerfile/default.env stay in sync
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Check documented vars have real defaults, and Dockerfile vars are documented
        run: bash .github/scripts/check-env-defaults.sh
